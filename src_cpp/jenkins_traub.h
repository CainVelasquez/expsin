#ifndef KEP_TOOLBOX_JENKINS_TRAUB_H
#define KEP_TOOLBOX_JENKINS_TRAUB_H

#include <cmath>
#include <vector>

namespace kep_toolbox{
  template<class funcSig>
  const std::vector<int>& jenkins_traub_roots(const std::vector<double> &roots, funcSig function, const int &max_iter, const double &precision){
    
  }
}

#endif
